{{- $tmpNodes := join ( getvs "/adding-hosts/kafka/*/instance_id") " " }}
TMP_NODES="{{  $tmpNodes  }}"
flush /opt/app/current/bin/envs/svc-caddy.env << CADDY_ENV_EOF
SERVICES="\$SERVICES caddy/{{ eq (getv "/env/web.console.enabled" "true") "true" }}/http:80"
CADDY_ENV_EOF
