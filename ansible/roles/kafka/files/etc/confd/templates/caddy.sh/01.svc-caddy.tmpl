{{- $tmpNodes := join ( getvs "/adding-hosts/kafka/*/instance_id") " " }}
TMP_NODES="{{  $tmpNodes  }}"
flush /opt/app/current/bin/envs/svc-caddy.env << CADDY_ENV_EOF
SERVICES="\$SERVICES caddy/{{ if eq (getv "/host/sid") 1 }}true{{else}}{{ eq (getv "/env/web.console.enabled" "true") "true" }}{{- end }}/http:80"
CADDY_ENV_EOF
