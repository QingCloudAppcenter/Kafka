{{ if eq $myRole "kafka" }}
flush > /opt/app/conf/jmx-exporter/exporter.yaml << JMX_EXPORTER_CONF_EOF
hostPort: 127.0.0.1:9999
{{ replace (getv "/env/jmx-exporter.conf" "rules:\n- pattern: \".*\"") `$` `\$` -1 }}
JMX_EXPORTER_CONF_EOF
{{ end }}